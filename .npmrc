# Ensure consistent package-lock.json across platforms
package-lock=true

# Use exact versions to avoid platform-specific resolution differences
save-exact=true

# Prefer online for consistent resolution
prefer-online=true

# Ensure consistent line endings in package-lock.json
lockfile-version=2
